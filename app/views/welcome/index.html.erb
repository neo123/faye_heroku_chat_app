<h1 align="center">Faye Chatting Application</h1>


<ul id="chat">
  <%= render @messages %>
</ul>

<%= form_for Message.new, :remote => true do |f| %>
  <%= f.hidden_field :username, :value => current_user.username %>
  <%= f.text_field :content %>
  <%= f.submit "Send" %>
<% end %>

